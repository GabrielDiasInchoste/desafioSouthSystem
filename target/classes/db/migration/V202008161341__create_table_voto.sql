CREATE TABLE VOTO(
	VOTO_ID INTEGER NOT NULL,
	PAUTA_ID INTEGER NOT NULL,
	ASSOCIADO_ID INTEGER,
	VOTO NUMBER(1,0) NOT NULL,
	FOREIGN KEY(PAUTA_ID) REFERENCES PAUTA (PAUTA_ID),
	FOREIGN KEY(ASSOCIADO_ID) REFERENCES ASSOCIADO (ASSOCIADO_ID),
    PRIMARY KEY (VOTO_ID)
)
